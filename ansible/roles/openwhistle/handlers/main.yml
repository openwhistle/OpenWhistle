---
- name: restart openwhistle
  ansible.builtin.systemd:
    name: openwhistle
    state: restarted
    daemon_reload: true

- name: reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

# Static files served directly by nginx for performance. Shared between the
# TLS (443) and onion (8080) server blocks — see proxy-headers.conf.
location /static/ {
    proxy_pass http://openwhistle;
    expires 30d;
    add_header Cache-Control "public, immutable";
}
